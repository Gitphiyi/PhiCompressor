[tool.poetry]
name = "src"
version = "0.1.0"
description = "Creating File Compressors and Comparing"
authors = ["Philip Yi <philip.yi@duke.edu>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
